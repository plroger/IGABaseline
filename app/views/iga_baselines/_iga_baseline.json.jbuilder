json.extract! iga_baseline, :id, :server_name, :ip, :os, :version_os, :qmgr, :version_mq, :broker, :version_broker, :comment, :created_at, :updated_at
json.url iga_baseline_url(iga_baseline, format: :json)
